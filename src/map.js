//运行的前十秒不做任何操作，之后每秒输出一个1
setTimeout(() => {
    let count = 1;
    const intervalId = setInterval(() => {
        // console.log(count);
        // count++;
        try{
            // console.log(player_position_global);
        }
        catch{
        
        }
        
    }, 1000);
}, 3000);
